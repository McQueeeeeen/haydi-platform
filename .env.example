# Public frontend values. Only VITE_* variables are exposed to browser code.
VITE_APP_URL="http://localhost:3000"

# Backend/runtime values for future server or edge functions.
# Keep real values in local .env files or Vercel environment variables.
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
